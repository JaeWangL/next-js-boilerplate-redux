export function* sample(): Generator {
  // yield put(articleSlice.actions.getArticlesFailure(e.message));
}

export default function* rootSaga(): Generator {
  // yield takeLatest('sample', sample);
}
